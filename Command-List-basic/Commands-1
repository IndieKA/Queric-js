// As converted from KAJS
// Example:
// var: define
// In this case, var would be "define" in Queric. So,
// instead of var awesomeness = 10;, we would have
// define awesomeness = 10;

// GLOBAL
- var: define
- true: true
- false: false

- this (as in this.playerX): global (as in global.playerX)
- function:
  function nameHere (parametersHere){// Stuff here};
  
- if: command
- case: part

- switch: shift or shiftBetween
- else: or
- break: end

